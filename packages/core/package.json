{
  "name": "@rocketship/core",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -b && cp -R src/schema dist/schema",
    "lint": "eslint --config eslint.config.cjs . --ext .ts,.mts",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "ajv": "^8.17.0",
    "opossum": "^9.0.0"
  },
  "devDependencies": {
    "madge": "^8.0.0",
    "ts-prune": "^0.10.3"
  }
}
